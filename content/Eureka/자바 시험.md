12문제
1 다음 자바 기본 타입 중 크기가 가장 큰 것은?
short long - byte int
2 heap에 생성되지 않은 객체를 참조할 때 발생하는 예외는
classcast io nullpointer - notreference
3 다음중 컴파일러가 java 코드에 자동으로 처리해 주는 작업이 아닌 것은?
	생성자가 없을 때 기본 생성자를 추가해 준다
	lang 및 io 패키지를 자동으로 import 해준다 -
	다른 클래스를 상속하지 않은 클래스에 object 클래스를 상속받도록 한다
	어떤 클래스를 상속받는 클래스의 생성자에 부모 생성자 호출이 없으면 super()를 넣어준다
4 다음 중 메소드 재정의 관련 올바르지 않은 것은?
	상속 받은 메소드의 접근 제한자를 더 제한할 수 없다
	리턴 타입이 primitive인 경우 동일해야 한다
	상속 받은 메소드의 발생 예외는 동일해야 한다 -
	@Override 를 사용하는게 코드 유지 보수에 좋다
5 다음은 자바 코드의 일부이다. 이 중 제네릭을 표현하는게 아닌 것은?
	public class MyClass<T>{}
	public interface MyIF<E, M>{}
	public <T> Box<T> boxing(T t){}
	public <Number extends T> boolean compare(T t1, T t2){} -
6 다음 중 예외에 대한 설명 중 올바르지 않은 것은?
	runtime 은 compiler에 의해 체크되지 않는다
	사용자 정의 예외는 ru
7
10
12
15
18
19