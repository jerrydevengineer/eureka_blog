12문제
1 다음 자바 기본 타입 중 크기가 가장 큰 것은?
short long - byte int
2 heap에 생성되지 않은 객체를 참조할 때 발생하는 예외는
classcast io nullpointer - notreference
3 다음중 컴파일러가 java 코드에 자동으로 처리해 주는 작업이 아닌 것은?
	생성자가 없을 때 기본 생성자를 추가해 준다
	lang 및 io 패키지를 자동으로 import 해준다 -
	다른 클래스를 상속하지 않은 클래스에 object 클래스를 상속받도록 한다
	어떤 클래스를 상속받는 클래스의 생성자에 부모 생성자 호출이 없으면 super()를 넣어준다
4 다음 중 메소드 재정의 관련 올바르지 않은 것은?
	상속 받은 메소드의 접근 제한자를 더 제한할 수 없다
	리턴 타입이 primitive인 경우 동일해야 한다
	상속 받은 메소드의 발생 예외는 동일해야 한다 -
	@Override 를 사용하는게 코드 유지 보수에 좋다
5 다음은 자바 코드의 일부이다. 이 중 제네릭을 표현하는게 아닌 것은?
	public class MyClass<T>{}
	public interface MyIF<E, M>{}
	public <T> Box<T> boxing(T t){}
	public <Number extends T> boolean compare(T t1, T t2){} -
6 다음 중 예외에 대한 설명 중 올바르지 않은 것은?
	runtime 은 compiler에 의해 체크되지 않는다
	사용자 정의 예외는 runtime 이 아닌 예외만 상속 받아 만들 수 있다 - 
	throw을 이용해서 다양한 객체를 생성하고 호출한 상위 메소드에 그 객체를 전달할 수 있다.
	메소드 선언 부분에 throws nullpointer를 하지 않아도 그 예외가 발생하고 처리하지 않으면 호출한 상위 메소드에 전달 된다
7 List 컬렉션에 대한 설명 중 틀린 것은
	주요 구현 Array, Linked
	멀티 쓰레드일때 vector가 더 안전
	Array 지우면 거기는 비워 둔다 -
	LinkedList는 중간에 삽입, 삭제가 많을 때 유리하다
10 다음 중 변수로 사용할 수 없는 것
	8hour -
	myName
	intArr
	_idx
12 접근제한자의 설명으로 올바른 것은?
	public 메소드는 throws 선언한 경우 빼고 모두 접근해서 호출이 가능하다
	default 메소드는 같은 패키지에서 호출할 수 있다. 단 default 키워드를 붙여야한다
	private field는 상속되지 않는다
	protected 메소드는 상속관계의 하위 클래스에서 호출이 가능하다 -
15 자바의 oop에 관련된 내용 중 올바른 것은?
	private 등의 접근제한자를 통해 메소드의 다형성을 구현할 수 있다.
	setter와 getter는 메소드 다형성의 주요한 역할을 한다
	자식이 재정의한 메소드는 자식 타입으로 선언된 변수를 통해서만 호출이 가능하다
	interface는 또 다른 interface를 상속받을 수 있다. -
18 다음 기본타입에 대응하는 wrapper class 중 올바르지 않은 것은?
	char - Char -
	int - Integer
	long - Long
	float - Float
19 다음 중 성격이 나머지와 다른 제어문은?
	for
	while
	do-while
	switch -